import React, {Component, Fragment} from 'react';
import FeaturesGridwallComponent from "../../../components/FeaturesGridwall/FeaturesGridwall/FeaturesGridwallComponent";

class FeaturesGridwall extends Component {   
    render() {
        const { legacyCloudFeatures = [], bannerYTTVContentData = {} } = this.props || {};
        return (
            <Fragment>
                <FeaturesGridwallComponent legacyCloudFeatures={legacyCloudFeatures} bannerYTTVContentData={bannerYTTVContentData}/>
            </Fragment>
        );
    }
}
const FeaturesGridwallEditConfig = {
    emptyLabel: "Features Grid Wall",
    isEmpty: function(props) {
        return true;
    }
};

export default FeaturesGridwall;
